/* Crear dos variables numéricas y utilizar el operador suma
para guardar el valor de la suma de ambos números en una 3er variable. */
console.log('------------------------------------------------------------\n1.a:')

var variableNum1 = 24, variableNum2 = 35, numResult;
numResult = variableNum1 + variableNum2;

console.log(numResult);

/* Crear dos variables de tipo String y concatenarlas guardando el resultado en una 3er variable.*/
console.log('\n------------------------------------------------------------\n1.b:')

var variableString1 = 'audio', variableString2 = 'visual', stringConcat;
stringConcat = variableString1 + variableString2;

console.log(stringConcat);

/* Crear dos variables de tipo String y sumar el largo de cada variable (cantidad de letras del string)
guardando el resultado de la suma en una 3er variable (utilizar length). */
console.log('\n------------------------------------------------------------\n1.c:')

var variableString4 = 'house', variableString5 = 'parrot', stringsLength;
stringsLength = variableString4.length + variableString5.length

console.log(stringsLength);